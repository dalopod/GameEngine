#pragma once

// Userinterface that displays health, mana, items, and other information in the bottom row.
class UserInterface
{
public:
	UserInterface();
	~UserInterface();

	void render();

private:
};

